/// Represents a single entry on the prediction leaderboard.
class LeaderboardEntry {
  final String name;
  final int points;

  const LeaderboardEntry({required this.name, required this.points});
}
